export type itemsTypes = {
    date: Date,
    category:string,
    title:string,
    value:number ,
    id:number
}

export type categoryType = {
    title:string,
    color:string,
    expense:boolean
    
}